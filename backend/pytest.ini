[pytest]
# Automatically treat all async test functions as asyncio coroutines.
# Required for @pytest.mark.asyncio tests to run without explicit markers
# on every function (pytest-asyncio >= 0.21).
asyncio_mode = auto

# Test discovery paths
testpaths = tests

# Show short traceback for cleaner output
addopts = -v --tb=short
